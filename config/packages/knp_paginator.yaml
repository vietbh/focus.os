knp_paginator:
    page_range: 3                       # Number of links shown in the pagination menu
    default_options:
        page_name: page                 # Page query parameter name
        sort_field_name: sort           # Sort field query parameter name
        sort_direction_name: direction  # Sort direction query parameter name
        distinct: true                  # Ensure distinct results (useful for GROUP BY)
        filter_field_name: filterField  # Filter field query parameter name
        filter_value_name: filterValue  # Filter value query parameter name
        page_out_of_range: ignore       # 'ignore', 'fix', or 'throwException'
        default_limit: 10               # Default number of items per page
    template:
        pagination: 'shared/pagination/tailwindcss_pagination.html.twig' # Default sliding pagination
        sortable: '@KnpPaginator/Pagination/sortable_link.html.twig' # Sort link template
        filtration: '@KnpPaginator/Pagination/filtration.html.twig' # Filter template
