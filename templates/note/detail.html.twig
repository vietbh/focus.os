{% extends 'base.html.twig' %}

{% block title %}
    {{ note.title }}
{% endblock %}

{% block body %}

    <twig:PageHeader
        title="{{ note.title }}"
        subtitle="Note"
    />

    <twig:Note:Detail :noteId="note.id" />

{% endblock %}
